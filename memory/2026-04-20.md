修复主页头像 WebP 图片质量问题

之前的 WebP 转换使用了默认质量设置（q=85），导致图片压缩过度，
头像出现失真和比例问题。

使用无损压缩重新生成：
- max-player.webp: 19KB -> 104KB
- max-creator.webp: 29KB -> 124KB

图片质量现在与原始 JPG 相同。

---

## 【事故记录】主页头像 WebP 处理失误与回滚 (2026-04-20 10:30-12:30)

**问题经过**：
1. 为优化加载性能，将主页头像 JPG 转为 WebP（cwebp q=85）
2. 用户反馈头像显示异常：比例不对、Alpha通道丢失、画面比例不对
3. 尝试恢复 JPG → 仍然不对（用户截图确认）
4. 发现根本原因：原始头像本身就是 WebP 格式（带Alpha透明），JPG 是fallback
5. 使用 cwebp 重新生成时丢失了原始 WebP 的透明通道和正确比例
6. 最终回滚到 9:27 版本 (16888a0)，放弃上午所有 WebP/懒加载相关修改

**根本原因**：
- 误解了文件格式历史。原始头像在 `758a2e3` 就是 WebP，后续 `1b6d8e9` 添加了 JPG fallback
- cwebp 工具重新生成时：
  - 丢失了 Alpha 透明通道（原图有透明背景）
  - 改变了图片比例（压缩参数导致）
  - 破坏了原有视觉效果

**正确做法（如果未来需要）**：
- 不要对已优化过的 WebP 再次用 cwebp 压缩
- 如需转换，使用 `cwebp -lossless` 或 `-q 100` 保持质量
- 或者直接保留原始文件，不做任何转换

**回滚范围**：
放弃了这些提交：
- `5683722` perf: 添加懒加载 + 图片转为 WebP 格式
- `7d1b91d` ui(word-alchemy): hero图改为顶部banner样式
- `bf1e7bb` fix(game-layout): hero banner全宽显示
- `6aa3741` fix(images): 重新生成无损WebP头像图片
- `443e021` revert(home): 恢复主页使用JPG头像
- `25e7a8b` fix(home): 恢复原始WebP头像文件

保留的提交：
- `16888a0` fix(word-alchemy): 同步游戏修复和卡片样式更新
- `a5e7559` ui(word-alchemy): hero图改为顶部banner样式（仅游戏页面，独立修改）

---

## 【后续修改】游戏页面 Hero Banner 独立调整 (2026-04-20 12:30)

**提交**: `a5e7559`

**修改内容**：
- `_layouts/game.html`: 调整容器布局，支持 hero 图全宽显示
- `_games/word-alchemy.md`: 移除 border-radius 和 max-width，让 hero 图占满宽度

**效果**：
- Hero 图上紧贴面包屑菜单，下紧贴内容介绍
- 宽度与导航栏一致
- 不影响主页和其他游戏页面

**访问地址**：https://hiyascott.github.io/hiyamax-home/games/word-alchemy/

---

## 【教训总结】2026-04-20

1. **不要对已优化的 WebP 再次压缩** — 会破坏原有质量
2. **Git 历史是最佳备份** — 原始文件永远可以从历史恢复 (`git show 758a2e3:file.webp`)
3. **修改前先确认文件原始状态** — 避免基于错误假设做修改
4. **用户说"不对"时立即停止** — 不要继续尝试修复，先回滚到稳定版本
5. **独立页面的样式可以单独控制** — game.html 的修改不影响 home.html

---

## 【技术决策记录】hiyamax-home 项目今日修改汇总

**时间**: 2026-04-20 上午
**项目**: hiyamax-home (Jekyll + GitHub Pages)
**仓库**: https://github.com/hiyaScott/hiyamax-home

### 修改1: 词语炼金术游戏 Bug 修复

**文件**: `games/word-alchemy/play/index.html`
**问题**: 三个bug
1. 重复图标（烟💨/机器⚙️/人造人15号🤖）
2. 手机端元素库只显示3行，无法滚动
3. 不能选相同元素两次（人+人=爱失败）

**修复**:
- 图标替换: 烟→💭, 机器→🏭, 人造人15号→🦾
- CSS 添加 `max-height: 340px` + `overflow-y: auto`
- 修改 `selectElement()` 函数，允许同一元素占两个槽位

### 修改2: 游戏卡片样式改为 artworks 风格

**文件**: 
- `_includes/game-card-full.html`
- `_games/word-alchemy.md`（添加 image + description）

**改动**: 从 emoji 卡片改为图片在上、文字在下的 artworks 风格

### 修改3: 懒加载 + WebP 格式转换

**文件**:
- `_includes/game-card-full.html` / `artwork-card-full.html`: `<img loading="lazy">`
- `_layouts/games-list.html` / `artworks-list.html`: 添加 `.card-img` CSS
-  artworks/*.jpg → .webp (4张)
- images/max-creator.jpg / max-player.jpg → .webp
- 4个 artworks 数据文件更新图片路径

### 修改4: Hero Banner 全宽显示（曲折）

**文件**: `_layouts/game.html`, `_games/word-alchemy.md`

**难点**: 多层容器 padding 限制全宽
```
.game-page { padding: 40px; }     ← 限制1
.game-wrapper { max-width: 600px; } ← 限制2
.game-container { padding: 40px; }   ← 限制3
```

**解决方案**:
1. 移除 `.game-page` 的 padding
2. `.game-container` 改为 `padding: 0 40px 40px`
3. 在 layout 中添加 `.wa-hero-wrapper` 全宽样式:
   ```css
   width: calc(100% + 80px);
   margin: 0 -40px;
   ```

### 修改5: WebP 压缩质量教训

**问题**: 默认 `cwebp -q 85` 对照片/人像压缩过度
- max-player.webp 仅 19KB，头像严重失真
- max-creator.webp 仅 29KB，颜色失真

**正确做法**: 使用无损压缩
```bash
cwebp -q 95 -lossless max-player.jpg -o max-player.webp
```
- max-player.webp: 19KB → 104KB
- max-creator.webp: 29KB → 124KB

**教训**: 人脸/照片类图片不能用低质量 WebP，必须使用无损或 q=95+。

### 工作目录确认

**正确目录**: `/root/.openclaw/workspace/hiyamax-home-jekyll/`
（不是 portfolio-blog/research/max-home/，那是旧项目）

### Git 提交记录
- `16888a0` fix(word-alchemy): 同步游戏修复和卡片样式更新
- `5683722` perf: 添加懒加载 + 图片转为 WebP 格式
- `7d1b91d` ui(word-alchemy): hero图改为顶部banner样式
- `bf1e7bb` fix(game-layout): hero banner全宽显示
- `6aa3741` fix(images): 重新生成无损WebP头像图片

---

## 【下午工作记录】词语炼金术游戏 Bug 修复与优化 (2026-04-20 14:00-16:30)

### 修复的 Bug 清单

| Bug | 原因 | 修复方式 | 提交 |
|-----|------|---------|------|
| 配方冲突 | '火+风' 同时映射到 '烟' 和 '能量' | '能量' 改为 '风+风=能量' | `4462594` |
| 缺失元素分类 | '泥浆'/'灰烬'/'房子' 不在 elementCategories | 添加到 '物质' 分类 | `4462594` |
| 同元素双选失败 | 第二次点击相同元素会取消选择 | 优先填入空槽位 | `4462594` |
| 导航栏重叠 | 关卡/得分与关闭按钮重叠 | 移除nav-bar中的关卡和得分 | `14646a0` |
| 得分显示位置 | 右上角被关闭按钮遮挡 | 移到目标区域（合成进度旁） | `b6ff390` |

### 代码修改细节

**1. 配方冲突修复**
```javascript
// 修复前
'火+风': '烟',
'火+风': '能量',  // 覆盖了上面的

// 修复后
'火+风': '烟',
'风+风': '能量',  // 使用不同的组合
```

**2. 缺失分类修复**
```javascript
elementCategories: {
    '物质': ['岩浆', '泥浆', '石头', '金属', '工具', '木材', '炭', '灰烬', '城堡', '能量', '房子'],
}
```

**3. 同元素双选逻辑**
```javascript
// 如果元素已在 slot1，且 slot2 为空，则填入 slot2
if (slots[0] === element && !slots[1]) {
    slots[1] = element;
    selectedSlot = 1;
    updateSlots();
    renderElements();
    return;
}
```

**4. 得分显示位置调整**
- 移除 nav-bar 中的"关卡"和"得分"
- 在目标区域添加：合成进度 0/1 | 得分 0

### 卡片懒加载优化（仅二级页面）

**提交**: `9d801b0`

修改内容：
- `_includes/game-card-full.html`: CSS背景 → `<img loading="lazy">` + `<picture>` WebP支持
- `_includes/artwork-card-full.html`: 同上
- `_layouts/games-list.html` / `artworks-list.html`: 添加 `.card-img` / `.card-bg` CSS
- artworks 4张图片转为 WebP 格式

**WebP 压缩结果**：
| 文件 | JPG | WebP | 节省 |
|------|-----|------|------|
| abstract-energy | 2.6MB | 2.4MB | ~7% |
| color-explosion | 1.4MB | 1.0MB | ~28% |
| color-palette | 1.9MB | 1.8MB | ~5% |
| panda-portrait | 1.7MB | 1.4MB | ~17% |

**注意**：主页头像不参与此优化，保持原始WebP不变。

### 最终提交记录

```
14646a0 fix(word-alchemy): 删除导航栏中的关卡显示
b6ff390 fix(word-alchemy): 调整得分显示位置避免与关闭按钮重叠
4462594 fix(word-alchemy): 修复3个游戏bug（配方冲突/缺失分类/同元素双选）
9d801b0 perf(cards): 二级页面卡片添加懒加载 + WebP 支持
16888a0 fix(word-alchemy): 同步游戏修复和卡片样式更新（上午）
```

---

## 【新项目】hiyaMax Home Jekyll 博客 (2026-04-20 16:30-19:00)

**项目位置**: `/root/.openclaw/workspace/hiyamax-home-jekyll/`
**GitHub仓库**: `hiyaScott/hiyamax-home`
**部署地址**: https://hiyascott.github.io/hiyamax-home/

### 项目架构

基于 Jekyll + GitHub Pages 的静态博客：
- 主页 (Player & Creator 双定位)
- 游戏作品集 (games/)
- 艺术作品集 (artworks/)
- 关于页面
- 导航与社交链接

### 迁移的游戏

1. **谁是卧底** (undercover)
   - 支持在线/本地两种模式
   - 本地版：支持 4-12 人，身份分配+投票
   - 在线版：Firebase 实时数据库
   - 详情页设计参考 Scott 提供的截图风格

2. **妈妈计数器** (mama-counter)
   - 核心功能：语音监听检测"妈妈"关键词
   - Web Speech API (webkitSpeechRecognition)
   - 手动添加模式
   - 统计功能：今日/本周/本月/历史最高/平均
   - 本地 localStorage 存储

3. **四象限法则** (eisenhower-matrix)
   - 紧急-重要矩阵任务管理
   - 拖拽分类
   - 本地存储

4. **密码生成器** (password-generator)
   - 多种密码类型
   - 强度评估
   - 一键复制

### 技术特点

- 响应式设计（桌面/平板/手机）
- Jekyll 模板系统（_layouts/, _includes/）
- YAML Front Matter 配置游戏元数据
- GitHub Actions 自动部署
- CDN 缓存（GitHub Pages 有 5-10 分钟延迟）

---

## 【技术实现】妈妈计数器语音监听功能 (2026-04-20 18:00)

**核心API**: Web Speech API - `webkitSpeechRecognition`

**实现要点**:
```javascript
// 1. 检测浏览器支持
const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
if (!SpeechRecognition) { /* 降级到手动模式 */ }

// 2. 配置中文识别
recognition.lang = 'zh-CN';
recognition.continuous = true;  // 持续监听
recognition.interimResults = false;  // 只返回最终结果

// 3. 关键词匹配
const keywords = ['妈妈', '麻麻', '妈咪', 'mama', '妈'];
results.forEach(result => {
    const transcript = result[0].transcript.toLowerCase();
    if (keywords.some(kw => transcript.includes(kw))) {
        incrementCounter();
    }
});
```

**兼容性**:
- Chrome/Edge: ✅ 完整支持
- Safari: ✅ 支持（需用户授权麦克风）
- Firefox: ❌ 不支持（降级到手动模式）

**隐私说明**: 语音识别在浏览器本地处理，数据不上传服务器。

---

## 【修复】妈妈计数器 Hero Banner 路径问题 (2026-04-20 19:00)

**问题**: 游戏内嵌在 Jekyll 站点的子路径 `/games/mama-counter/play/`，但 hero banner 图片使用绝对路径 `/games/mama-counter/play/banner.webp`，在子目录部署时可能 404。

**修复**: 将路径改为相对路径 `./banner.webp`，确保无论部署在什么子路径下都能正确加载。

**注意**: GitHub Pages CDN 有缓存延迟，修改后需要 2-3 分钟才能在线上生效。

---

## 【待处理】妈妈计数器详情页设计 (2026-04-20 19:20)

**状态**: 等待 Scott 确认需求

Scott 发送了"谁是卧底"详情页的截图，询问是否要将妈妈计数器也做成类似风格。尚未收到明确答复。

**可能的需求方向**:
1. 为妈妈计数器创建独立的详情页（类似谁是卧底的结构）
2. 仅作为参考展示，不需要修改
3. 其他意图

**下一步**: 等 Scott 明确说明后再行动。